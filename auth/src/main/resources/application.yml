spring:
  application:
    name: auth
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/my_example?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 30000

