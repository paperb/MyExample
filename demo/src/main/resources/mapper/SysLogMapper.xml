<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.SysLogDao">

    <insert id="saveSysLog" parameterType="com.example.demo.entity.SysLog">
        insert into sys_log values(null,#{methodName},#{param},#{result},#{createTime})
    </insert>

</mapper>
